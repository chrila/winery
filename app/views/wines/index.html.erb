<h1>Wines</h1>
<table>
  <thead>
    <tr>
      <td>Id</td>
      <td>Name</td>
      <td></td>
    </tr>
  </thead>
  <tbody>
    <% @wines.each do |w| %>
      <tr>
        <td><%= w.id %></td>
        <td><%= w.name %></td>
        <td>
          <%= link_to 'Show', w %>
          <%= link_to 'Edit', edit_wine_path(w) %>
          <%= link_to 'Delete', w, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Add wine', new_wine_path %>
