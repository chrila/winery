<h1>Wines</h1>
<table>
  <thead>
    <tr>
      <td>Id</td>
      <td>Name</td>
      <td>Grapes</td>
      <td></td>
    </tr>
  </thead>
  <tbody>
    <% @wines.each do |w| %>
      <tr>
        <td><%= w.id %></td>
        <td><%= w.name %></td>
        <td>
          <ul>
            <% w.grape_wines.ordered.each do |gw| %>
              <li><%= gw %></li>
            <% end %>
          </ul>
        </td>
        <td>
          <%= link_to 'Show', w %>
          <%= link_to 'Edit', edit_wine_path(w) %>
          <%= button_to 'Delete', w, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Add wine', new_wine_path %>
